# LLM providers (at least one required)
huggingface-hub>=0.20.0  # Primary provider (free/cheaper)
openai>=1.0.0            # Optional fallback provider

# HTTP and API clients
requests>=2.31.0
beautifulsoup4>=4.12.0

# Configuration management
Markdown>=3.5.0

# Utilities
python-dotenv>=1.0.0
modal>=0.62.0

# Testing
pytest>=7.4.0

# Development
black>=23.0.0
ruff>=0.1.0

fastapi[standard]
supabase>=2.0.0
# Deep web research - stealth HTTP scraping (bypass anti-bot, full article content)
scrapling[fetchers]>=0.4.0
