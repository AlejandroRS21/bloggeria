schema: spec-driven
context: |
  Tech stack: Blogger Agent TFG — Python 3.11 backend (8 agents: StyleAnalyzer,
  KeywordExtractor, ContentGenerator, Critic, ImageSelector, HTMLBuilder,
  AnonymousBloggerEmulator, StyleExtractor; 4 LLM providers: HuggingFace, OpenAI,
  Gemini, Modal GPU; orchestrator 7 fases) + Next.js 16 frontend (React 19,
  Tailwind CSS 4, App Router) + Supabase Postgres (tabla posts) + Modal serverless
  webhook + Vercel deploy. CI: GitHub Actions (backend pytest+ruff, frontend
  build+typecheck, Playwright E2E). Async post generation con job_id + polling
  Supabase. Fuente de verdad: engram (memoria persistente).
sdd:
  execution_mode: auto
  artifact_store: engram
  delivery_strategy: ask
  review_budget: 400
  preflight_done: true
